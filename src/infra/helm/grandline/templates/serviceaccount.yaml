{{- if .Values.serviceAccount.create }}
apiVersion: v1
kind: ServiceAccount
metadata:
  name: {{ include "grandline.serviceAccountName" . }}
  labels:
    {{- include "grandline.labels" . | nindent 4 }}
{{- end }}
